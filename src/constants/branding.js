/**
 * branding.js — Centralized Branding Constants
 * 
 * MEDROUTER Brand Identity
 */

export const BRAND_NAME = "MEDROUTER";
export const BRAND_TAGLINE = "Routes That Saves Lives";
export const BRAND_FULL = `${BRAND_NAME} — ${BRAND_TAGLINE}`;

// For AI Copilot signature
export const BRAND_SIGNATURE = `Powered by ${BRAND_NAME} — ${BRAND_TAGLINE}`;

// For meta/SEO
export const BRAND_DESCRIPTION = `${BRAND_NAME} is an AI-powered ambulance triage and hospital routing platform that optimizes emergency response in real time. ${BRAND_TAGLINE}.`;
